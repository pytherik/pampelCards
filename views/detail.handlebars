<div class="home-container">
  <div class="card-container">
    <div id="question-container" class="task-container">
      <div class="random-question">
        <hr class="card-line">
        <h1 id="card-question">{{ card.question }}</h1>  
        <hr class="card-line">
        <span id="card-category">Kategorie: {{ card.category }}</span>
      </div>
      <div>
      <h5 class="">{{ card.description }}</h5>
      {{{ card.sanitizedHtml }}}
      </div>
    </div>
    <div class="button-footer">
      <a class="button btn-blue" href="/card/list/{{card.category}}">zurück</a>
    </div>
  </div>
</div>
